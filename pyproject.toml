[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "expense-tracker"
version = "0.1.0"
description = "An expense tracker built to learn OOP and SOLID principles"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]